[build]
  command = "curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install && ~/.bun/bin/bun run build"
  publish = ".output/public"

[build.environment]
  BUN_INSTALL = "/opt/build/.bun"

[env.production]
  NODE_ENV = "production"
